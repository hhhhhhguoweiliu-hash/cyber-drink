# V7 3D 本地启动

本分支的 `drink-3d` 与 `bartend-3d` 使用微信小程序官方团队维护的 `threejs-miniprogram` WebGL 适配层。

首次拉取本分支后：

1. 在项目根目录执行 `npm install`。
2. 用微信开发者工具打开项目。
3. 菜单选择 **工具 → 构建 npm**。
4. 重新编译小程序。
5. 首页选择任意酒进入 `3D 赛博酒桌`；鸡尾酒会进入 `3D 调酒台`。

## 当前 3D 范围

- 实时透视相机、灯光、阴影和酒桌场景。
- 6 类酒的程序化 3D 酒瓶/酒杯。
- 3D 倒酒：瓶身倾斜、液柱、杯内液面增长。
- 3D 饮用：举杯、倾斜、液面减少、放杯。
- 可拖动场景改变观察角度。
- 3D 调酒：Jigger 量酒、加冰、加辅料、Shaker 摇壶、滤冰出酒、Garnish。

## 资产策略

当前先使用程序化低面数 3D 几何体保证小程序性能与包体。后续高拟真版本再逐步替换为经过压缩的 GLB/PBR 模型，并优先考虑分包或远程资源，避免挤占主包空间。
